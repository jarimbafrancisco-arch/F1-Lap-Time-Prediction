# Progress Log

## Initial environment setup and event selection

### Work completed
Created a Python virtual environment and connected it to a VS Code
notebook. Installed FastF1 and ipykernel, then retrieved the 2024
event calendar, excluding testing events.

### What I learned
A virtual environment isolates the project's dependencies.
The notebook kernel determines which Python environment executes
the code. Retrieving the event calendar does not load lap data.

### Initial decision
Selected the 2024 Italian Grand Prix at Monza as the first sample
for exploring FastF1. This is an exploratory choice; the final
dataset and features have not been selected.

### Next step
Load the race session and inspect the available lap data.